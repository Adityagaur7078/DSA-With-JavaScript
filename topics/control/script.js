const topic = "switch";


if (topic === "sum") runSum();
if (topic === "typeCoercion") runTypeCoercion();
if (topic === "typeCasting") runTypeCasting();
if (topic === "swapping") runSwapping();
if (topic === "operators") runOperators();
if (topic === "math") runMathFunctions();
if (topic === "switch") runSwitch();